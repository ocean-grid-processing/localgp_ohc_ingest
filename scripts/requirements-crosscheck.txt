# Python deps for the ohc_ingest tooling: publish.py, verify_store.py, verify_publish.py.
# Transitive deps (pandas, numcodecs, donfig, ...) are resolved automatically.
numpy
scipy             # verify: scipy.io.loadmat (independent .mat oracle)
xarray>=2024.10   # zarr-v3 support
zarr>=3           # reads the v3 store + registers the xarray zarr engine
netCDF4           # publish: writes the compliant .nc submission
